add_three <- function(input){
return_value() <- input + 3
}
ten <- add_three(7)
add_three <- function(input){
return_value <- input + 3
}
ten <- add_three(7)
print(ten)
imperial_to_metric <- function(feet, inches){
feet <- 5
inches <- 6
total_inches <- feet * 12 + inches
meters <- total_inches * 0.0254 # from google
}
imperial_to_metric(5,6)
imperial_to_metric(5,6)
imperial_to_metric <- function(feet, inches){
total_inches <- feet * 12 + inches
meters <- total_inches * 0.0254 # from google
meters
}
imperial_to_metric(5,6)
dogs <- c("Fido", "Spot", "Sparky")
dogs
one_to_hundred <- 1:100
one_to_hundred
v1 <- c(3,1,4,1,5)
v2 <- c(1,6,1,8,0)
v3 <- v1 + v2
v3
v1 <- c(3,1,4,1,5)
v2 <- c(10, 100)
v3 <- v1 + v2
source('C:/Users/tedxu/Desktop/learning-git/book-exercises/chapter-06-exercises/exercise-1/exercise.R', echo=TRUE)
# Create a variable `now_doing` that is your name followed by "is programming!"
# (use the `paste()` function)
now_doing <- paste(my_name,"is programming！")
# Make the `now_doing` variable upper case
toupper(now_doing)
source('C:/Users/tedxu/Desktop/learning-git/book-exercises/chapter-06-exercises/exercise-1/exercise.R', echo=TRUE)
# Create a variable `raw_sum` that is the sum of the two variables. Use the
# `sum()` function for practice.
raw_sum = sum(fav_1, fav_2)
raw_sum
# Create a variable `round_sum` that is the `raw_sum` rounded to 1 decimal place.
# Use the `round()` function.
round_sum <- round(raw_sum, digits = 1)
raw_sum
round
round_sum
# Which is bigger, `round_sum` or `sum_round`? (You can use the `max()` function!)
max(round_sum, sum_round)
# Which is bigger, `round_sum` or `sum_round`? (You can use the `max()` function!)
max(round_sum,sum_round)
# Which is bigger, `round_sum` or `sum_round`? (You can use the `max()` function!)
bigger <- max(round_sum,sum_round)
